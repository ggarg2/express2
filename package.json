{
  "name": "express-webservices",
  "version": "0.0.1",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "nodemon app",
    "start:local": "export NODE_ENV=default||set NODE_ENV=default&& node app",
    "start:dev": "export NODE_ENV=dev||set NODE_ENV=dev&& node app",
    "start:stage": "export NODE_ENV=stage||set NODE_ENV=stage&& node app",
    "start:prod": "export NODE_ENV=prod||set NODE_ENV=prod&& node app",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ggarg2",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.18.2",
    "config": "^1.30.0",
    "cors": "^2.8.4",
    "express": "^4.16.2",
    "mongoose": "^5.0.9"
  }
}
